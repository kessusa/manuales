"""
Add these path() entries to your existing rss/urls.py.
"""
from django.urls import path
from . import views

new_batch_routes = [
    # Batch move (POST with file_ids[] and target_folder_id)
    path(
        'file_manager/move_selection/',
        views.move_files_batch,
        name='move_files_batch',
    ),

    # Batch delete (POST with file_ids[])
    path(
        'file_manager/delete_selection/',
        views.delete_files_batch,
        name='delete_files_batch',
    ),
]
